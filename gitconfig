[user]
	name = Jakob Külzer
	email = jakob.kulzer@gmail.com

[alias]
  st = status
  co = checkout
  br = branch
  d = diff
  rb = rebase
  gn = grep -n
  gni = grep -ni
  l = log
  lp = log -p
  ln = log --name-status
  cp = cherry-pick
  ci = commit
  civ = commit --verbose
  pp = pull --prune
  wip = commit -m "wip"

[push]
	default = nothing

[core]
	excludesfile = ~/.gitignore
  editor = vim

[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true

[diff]
  algorithm = patience

[pager]
  branch =
